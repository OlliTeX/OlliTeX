// Main Typst document.
//
// To include more files (e.g. `#cite` a reference or pull in a chapter),
// `#include` them here. The include below is left commented so the very
// first compile succeeds out of the box — uncomment it and create
// `introduction.typ` in the project folder to add a real include:
//
//   #include "introduction.typ"

= <%= project_name %>

Insert your text here.
