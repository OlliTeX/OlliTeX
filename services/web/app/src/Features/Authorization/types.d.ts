// Re-export shim — the canonical types now live in services/web/types/api/authorization.d.ts
// (P7 reorg: shared types moved into the kept types/ home so the Node backend can be junked).
export * from '../../../../types/api/authorization'
