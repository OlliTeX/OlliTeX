# /go/libraries — Node→Go 1:1 library ports (HANDOFF — keep current every package)

**Directive (owner, 2026-09-20):** 1:1 drop-in **GOLANG** conversion of everything under
`libraries/*` into `/go/libraries/*`, stepping up tests: strict test suite per package
with detailed coverage. Keep this doc + the `.pi/todos/` ledger (LIB-01..16,
TODO ID `b5890b9a` = index) updated at every boundary so a fresh session takes over
seamlessly.

**Repo:** `/home/davrot/project-history/OlliTeX-ph`, branch `golang-ph`, Go module
`ollitex` (root `go.mod`, go 1.27). New packages land under `go/libraries/<name>/`.
Build gate per package: `go build ./... && go vet ./... && gofmt -l go/libraries && go test ./libraries/... -cover` (run from
repo root). Commit per package **green** (build+vet+gofmt+tests).

## STATUS (first line updates on any change)

`STATUS: 2026-09-21 — LIB-01 oerror DONE (96.8%); LIB-02 SKIP (Node plumbing); LIB-03 streamutils DONE (98.6%); LIB-04 validtools DONE (98.9% with ArrayVal/UnionVal composites); LIB-05 settings DONE (98.6%); LIB-06 rangestracker DONE (97.0%); LIB-07 fetchutils DONE (94.9%); LIB-08 accesstoken DONE (95.4% — the failing golden test used a wrong TEST password: Node suite uses 38 '4's, not 46); LIB-09 notifprefs DONE tests-green (69.2%); LIB-10 rediswrapper DONE (95.3% — Driver seam, oracle fully mock-based so NO live redis needed for unit tests); LIB-11 mongoutils+mongowrapper DONE (mongo-utils 88.0% ObjectId/testutils/batchedUpdate + mongoose-wrapper 92.5% façade; batchedUpdate live tests against real mongod via a `Mongo` seam); LIB-12 object-persistor DONE (88.6% — S3Client/GCSStorage seams, fully mock-based, no live cloud; gunzip fix zlib→gzip); LIB-13 ologger DONE (94.2% — Bunyan/Logger/Checker seams, mock-bunyan oracle mirrors all 31 Node tests; req serializer lockdown raw-params path + GCP converter + file/GCE log-level checkers + ring buffer + a real JSON-line logger all covered); LIB-14 ometrics DONE (95.9% — real prometheus-flavoured registry over stdlib: Counter/Gauge/Summary(quantiles+_sum)/Histogram(cumulative `le`+`+Inf`), default host/app labels + `buildPromKey` escape, the index API (inc/count/summary/timing/histogram/gauge/globalGauge/Timer) and http/event_loop/mongodb/open_sockets/memory/leaked_sockets monitors behind NARROW seams for the platform data sources; Go suite mirrors all 37 Node oracle tests); LIB-15 otc Phase A (OT core) DONE (91.9% — the OT text-operation primitives 1:1: Range, ScanOp/RetainOp/InsertOp/RemoveOp, TextOperation retain/insert/remove/apply/invert/compose/transform, TrackingProps/ClearTrackingProps, Comment/CommentList, TrackedChange/TrackedChangeList, StringFileData, the UnprocessableError/ApplyError/InvalidInsertionError/TooLongError family; the Go fuzz invariants (apply/invert/compose/transform) mirror the Node 500-trial randomised oracles); LIB-15 otc Phase B slice 1 (file-tree value model) DONE (92.9% cumulative — text_file_defaults, Author/AuthorList assertV1/V2, Label, file_metadata (isDocumentMetadata/withDocumentMetadataFlag/hasDocumentMetadataFlag), safe_pathname (clean/cleanDebug/isClean with the exact reason strings + a 1:1 posix path.normalize reproduction), file_type_detector (isTextFilename/needsContent/isEditableString/detectBuffer/detectFile — Node fs + utf-8-validate port to Go os + utf8.Valid)); LIB-15 otc Phase B slice 2 (Blob + blob utils) DONE (92.6% cumulative — `Blob` model (HEX_HASH_RX 40-hex + MAX_EDITABLE_BYTE_LENGTH_BOUND 3·MaxStringLength + EMPTY_HASH git empty-blob + NotFoundError) + `blob_utils` git blob hash (blobHashFromString/Buffer/Stream/File = sha1("blob "+byteLength+"\0"+content), matching git `hash-object` and the GitHub-reported shas; getStringLengthOfBuffer/File via utf8.Valid ≤MaxStringLength no-NUL no-non-BMP; blobForFile)); LIB-15 otc Phase B slice 3 (EditOperation family + BlobStore seam) DONE (90.0% package coverage — the EditOperation interface + the text wrapper (Phase A *TextOperation behind textEdit) + AddCommentOperation/DeleteCommentOperation/SetCommentStateOperation + EditNoOp + EditOperationBuilder.fromJSON/isValid + EditOperationTransformer.transform/transformMultiple (text+comment matrix, comment-range rebasing e.g. 10->16, the detached-comment case, and the U+FFAE "Transform not implemented" message all oracle-pinned) + the BlobStore seam (BlobStore interface + BaseBlobStore = BlobStoreBase defaults) + StringFileData.edit(editOperation)); LIB-15 otc Phase B slice 4 (File/FileData/FileMap backbone) DONE (87.6% package coverage — the `FileData` interface (Node null→nullable pointer, throw→error) + `HashFileData`/`BinaryFileData`/`HollowStringFileData`/`HollowBinaryFileData` + `StringFileData` MIGRATED onto the interface (UTF-16 string length, `putString`+`putObject` store, nullable accessors) + `LazyStringFileData` (toRaw/fromRaw/toEager/edit/store + the OError.tag error annotations, all oracle-pinned incl. blobHash/blobContentLength/metadataStringLength/firstOpBaseLength/operationIndex/currentContentLength) + `File` (fromString/fromHash/createHollow/createLazyFromBlobs, ToRaw/ToStats/Store deep-clone, Edit/Load/LoadEager/Clone, NotEditableError) + `FileMap` (add/remove/move/count/get/pathnames/map/mapAsync/wouldConflict + the 4 pathname error types) built on Phase B slices 1-3, so otc now models the full file-tree VALUE layer); LIB-15 otc Phase C slice 1 (Operation family + Change + Snapshot + rebase + Origin/V2DocVersions) DONE (88.6% package coverage — the file-tree `Operation` interface + NoOperation/AddFileOperation/MoveFileOperation/EditFileOperation/SetFileMetadataOperation + the full 4×4 transform matrix (the 12 named transform functions + the transpose rule Node `transpose(F)=F(b,a).reverse()`, all pinned to the Node oracles), Change (fromRaw/toRaw/pushOperation/findBlobHashes/applyTo recoverable-vs-strict/transformAfter/clone/store+dedupeAuthors/canBeComposedWith), Snapshot (fromRaw/toRaw/addFile/moveFile/editFile+EditMissingFileError/getPathnameWithDocFlag doc-only rules/applyAll/findBlobHashes/loadFiles/store/clone), rebaseChanges (transform over concurrent changes + no-op pruning + preserves origin/v2Authors/timestamp), and Origin/RestoreOrigin/RestoreFileOrigin/RestoreProjectOrigin + V2DocVersions; the Node 42-test origin/snapshot/change/rebase suite + 57-test operation/transform suite all mirrored green in Go); remaining Phase C (change_identity + doc_updater_compatible_ranges + file_tree_diff DONE in slice 2 (89.0%) and root_doc DONE in slice 3 (89.1%); diff_as_text_operation (DMP via go-diff) + build_set_content_operations DONE in slice 4 (89.0%; the `sergi/go-diff@v1.4.0` DMP pin is owner-approved and interop-verified against Node diff-match-patch@1.0.5 — see §Decisions). remaining: History/Chunk/ChunkResponse/OTClient/schemas/ot_migration_stages/change_note/change_request). Committed: L01,L03,L04,L05,L06,L07,L08,L09,L10,L11,L12,L13,L14,L15-A,L15-B1,L15-B2,L15-B3,L15-B4,L15-C1,L15-C2,L15-C3,L15-C4 (per-package, explicit staging). Next: LIB-15 otc Phase C (history/chunk/ot_client/schemas/ot_migration_stages/…) → LIB-16 strict gate (≥85 overall); notifprefs 69.2% below target must be raised or waived before the gate.`


| # | Package | Go pkg | Status | Coverage |
|---|---------|--------|--------|----------|
| 01 | o-error | oerror | ✅ | 96.8% |
| 02 | promise-utils | — | SKIP (Node plumbing — see §Decisions) |
| 03 | stream-utils | streamutils | ✅ | 98.6% |
| 04 | validation-tools | validtools | ✅ | 98.9% |
| 05 | settings | settings | ✅ | 98.6% |
| 06 | ranges-tracker | rangestracker | ✅ | 97.0% |
| 07 | fetch-utils | fetchutils | ✅ | 94.9% |
| 08 | access-token-encryptor | accesstoken | ✅ | 95.4% |
| 09 | notification-preferences | notifprefs | ✅ | 69.2% |
| 10 | redis-wrapper | rediswrapper | ✅ | 95.3% |
| 11 | mongoose-wrapper + mongo-utils | mongoutils + mongowrapper | ✅ | 88.0% / 92.5% |
| 12 | object-persistor | persistors | ✅ | 88.6% |
| 13 | logger | ologger | ✅ | 94.2% |
| 14 | metrics | ometrics | ✅ | 95.9% |
| 15 | overleaf-editor-core | otc (multi-phase) | ◐ Ph A+B1+B2+B3+B4+C1+C2+C3+C4 (DMP) | 89.0% |
| 16 | STRICT GATE + final handoff | — | ○ | target ≥85 overall |
| — | eslint-plugin | — | SKIP (ESLint-rules API, no Go analogue; decision documented §Decisions) |
| — | cypress-pnp-reporter | — | SKIP (Mocha reporter shim; same) |

○ open · ◐ in progress · ✅ done

**Order rationale:** dependency-first (o-error is imported by 8 of the 16 Node libs;
validation-tools next since logger/validtools/fetch all build on it + o-error).
Big package otc last; coordinates with ph-go `internal/opmodel` (see LIB-15 note).

## ENVIRONMENT (verified 2026-09-20)

- Go: **go1.27.0 linux/amd66** (repo go.mod `go 1.27`; module `ollitex`).
  `go build ./... && go vet ./... && gofmt -l . && go test ./... -cover` green at repo
  HEAD before start (267 .go files; go/README.md invariants apply: same env vars, same
  wire shape). ph-go sibling (`services/project-history.go`) has its own module
  `project-history` (go.work? NO — two independent modules; cross-imports via relative
  paths are NOT possible; if a lib needs a ph-go internal, re-implement — document).
- Docker (SHSTK workaround mandatory on this kernel):
  `docker run -d --name ph-mongo -p 27017:27017 -e GLIBC_TUNABLES=glibc.cpu.hwcaps=-SHSTK mongo:8.3`
  `docker run -d --name ph-redis -p 6379:6379 redis:7.4.0`
  (for LIB-10/11 live tests; containers may need `docker start` if restarted).
- Node v24.13.0, yarn PnP — the Node test suites (`yarn test` per lib, mocha/vitest)
  are the **oracle**; generate goldens from them before/while porting where cheap.
- Repo go.mod currently only has `go-mail`, `mongo-driver`, `x/crypto`. **Adding Go
  deps requires owner-visible rationale** (document in §Decisions).

## DECISIONS LOG (append-only)

- (LIB-03) **streamutils shape**: Node's bidirectional Transform/PassThrough
  stream ports to `io.Reader` wrappers over the source stream (Go request-body
  pipelines are read-side): `LimitedReader`, `TimeoutReader`, `LoggerReader`,
  `MeteredReader`. `WritableBuffer`/`ReadableString` are direct ports.
  Error surfaces: `*SizeExceededError` (msg pinned byte-for-byte: "exceeded
  stream size limit of %d: %d") + `*AbortError` ("stream timed out").
  `Meter`/`Logger` are NARROW local interfaces — the ometrics (LIB-14) and
  ologger (LIB-13) ports implement/buy them at their boundaries (no
  premature coupling).
  **IncrementalResponse** — Node `AbortController`→Go `context`, Node `res`
  →narrow `ProgressResponse{Write,Finish}`; the `#humanReadableTimeout` pin
  (Node operates in *ms* — its `timeout` is the setTimeout value, NOT a
  pre-converted Duration) is exercised byte-for-byte ("2min3s450ms" style).
  All error-message / log-shape pins match the Node test suite verbatim.
- (LIB-01) **oerror API surface**: Node exports ONLY `OError` (default export). Go exports: `OError{Type,Message,Info,Cause,Tags}`, `New(msg, info, cause...)` / `Of(msg)`, `(.WithInfo/.WithCause/.WithName/.Tag)`, `Tag(err,msg,info) error`, `GetFullInfo(error) map[string]any`, `GetFullStack(error) string`, `MaxTags`, `DroppedTags`, `TaggedError`. `Unwrap()` follows an error-typed cause for stdlib interop.
- (LIB-01) **Pinned divergences (HANDOFF §Divergences)**:
  (a) Node V8 stacks are not representable in Go: `Tag`'s `Error.captureStackTrace` half is dropped; `TaggedError` value (message+info) is the preserved surface and `GetFullStack` renders exactly the Node-pinned line structure (name:message / TaggedError: msg / caused by: chain, 4 spaces per cause depth).
  (b) Node `tag` accepts *any object* (even `{message:'Foo'}`); Go `Tag` on any error wraps in `*OError` (renders `"OError: <Error() text>"`) so repeated tags accumulate (Node singleton behaviour preserved via pointer identity).
  (c) Node monkey-patches plain `Error` instances (`.info=`, `.cause=`); Go equivalent is `New(...).WithInfo/.WithCause`.
  (d) `IsDropped` is structural (message + nil info), not reference `=== DROPPED_TAGS_ERROR` (Go struct comparison with maps is impossible).
  (e) Cap semantics ground-traced with Node itself: `[t1, DROP, t4, t5]` at maxTags=3, `[L0, DROP, L9, L10]` at 11-level recursion — pinned in `TestTagCap*`.
- (LIB-02) **promise-utils SKIP (Node plumbing, no Go drop-in)**: every export is
  JS promise/callback duality machinery (promisify/promisifyAll/promisifyClass,
  callbackify*/expressify/expressifyErrorHandler — Node `node:util` re-exports and
  Express middleware wrappers) plus `pLimit`. Go has no promise objects; its native
  contract IS the already-async function + error surface, and the repo's Go services
  already express each pattern idiomatically at use sites (e.g. `go/pbhttp` owns the
  middleware-shape bits). Porting this would create a dead wrapper layer that the Go
  services would NOT call — worse than a skip. Documented, NOT ported; every consumer
  that needs the *limit-map* idiom uses a Go waitgroup + buffered channel (document at
  consumer, e.g. when CLSI Go port lands).
- (init) Skip eslint-plugin + cypress-pnp-reporter from the Go tree: they implement
  the ESLint/Mocha *tool* APIs (rule definitions, Mocha reporter lifecycle); no runtime
  value in Go, and their "port" would be a dead abstraction. Both documented in HANDOFF
  at gate (LIB-16) as deliberate skips.
- (init) Package naming: Go-idiomatic short names (oerror, promiseutils, …), **not**
  npm names — npm names are the *spec*; Go name is the API. Directory = package path
  `go/libraries/<name>/`.
- (LIB-04) **validtools = hand-rolled zod 4.1.11 1:1 port (NO ZOG)**. Wire parity: Go `Val` interface + `Issue{Message,Code,Path}` mirror zod's `issue.message/issue.code/issue.path` byte-for-byte; `ZodError{Message:"Invalid input"}` → `zve.formatZodErrorLite` → `FriendlyMessage` produce the identical `Validation error: ...` text (see `wire_test.go` vs the 85 goldens). Leaf `refine` messages are oracle-pinned (absent → "received undefined", null → "received null", wrong-type → primitive typing issue). **Multi-file split** per user request: doc/issue/wire/types/datetime/validators/object/validate/errors/handler (+3 test files). NO new Go deps (zod regexes hand-written, datetime `time.Parse` + fallback). Coverage 99.6% (46 uncovered-blocks from the baseline all closed by `coverage_test.go` absent/null/wrong-type matrices + `time.Time`/`Local` arms).
- (LIB-05) **settings = 1:1 port of `merge.js` + `Settings.js`** into `go/libraries/settings`. `merge` is a faithful recursive fold: falsy/absent default slot → fresh `{}` (oracle: 0/""/false/null/absent all yield `{"o":{"a":1}}`); truthy-PRIMITIVE slot → override silently dropped (Node `<prim>[k]=v` no-op, slot survives); arrays/primitives replace wholesale; identity contract = returns the SAME defaults map (mutates in place — Go `reflect.Pointer` pins aliasing). JS `null` override → `ErrNullOverride` + `NullOverrideError{Path}` (Node raw TypeError, no path — path is a Go convenience). `Load` mirrors Settings.js: env mismatch throw, `cwd/{cjs,js}` → entry-dir resolution, NODE_ENV lowercasing, `mergeWith` (R11) dispatch checked on the DEFAULTS module, flying-blind `{}`. Seams: `EnvLookup`, `Filesystem`, `ReadModule`, `*LoadOpts`. Coverage 98.6% (foldLayer `default` arm + jsFalsy int8..uint64 arms + isNonNegativeInt closed by `coverage_test.go`).
- (LIB-06) **rangestracker = 1:1 port of `libraries/ranges-tracker`** (index.cjs 808LoC + schemas.js) into `go/libraries/rangestracker`. Node duck-typed ops → single `Op` struct with presence-pointer fields; applyOp dispatch order I→D→C matches Node (`op.i`/`op.d`/`op.c`). **Live-reference parity**: Node hands out the SAME objects (dirty state, `.changes` elements); Go stores `[]*Change`/`[]*CommentItem` internally and dirty entries are `*RangeRef` live pointers — a captured dirty ref sees later ops (TestDirtyRefsAreLive pins it); value slices are box/copy at the public boundary (New, GetChanges). **sameUser** replicates JS `===` on (absent|null|value): absent≡absent→true, absent-vs-null→false, DeepEqual otherwise. `pickTimestamp` strict `<` (tie→new), ts accepts *time.Time/RFC3339 string, unparseable→new (Node Invalid Date). AddComment: `op.t || newId()` — EMPTY STRING t is falsy → fresh id (pinned). **schemas.js** ports onto validtools as `InsertOpSchema…RangesSchema` + `ParseRanges` (Node safeParse). **validtools gained 2 framework composites** (`compose.go`): `ArrayVal{Item Val}` (z.array) and `UnionVal` (a.or(b) — first clean arm wins; all-fail → single invalid_union issue with per-arm groups, same wire as the DT union). Documented here because L15 otc / docstore schemas will want them too.
- (init) "1:1" granularity rule: exported function/class names map 1:1 (Go export
  convention, e.g. `OError` → `OError`); behaviour (messages, status codes, error
  shapes, edge cases) is byte-for-byte where deterministic. Non-Go-isms (dual stacks,
  `undefined` vs `nil`) get the narrowest faithful equivalent + test pin.
- (LIB-07) **fetchutils = 1:1 port of `libraries/fetch-utils/index.ts`** (~450 LoC) into `go/libraries/fetchutils` (fetchutils.go core/errors/headers/watchdog/bodyPipe, agent.go Custom{Http,Https}Agent, variants.go fetchJson/fetchStream/fetchNothing/fetchRedirect/fetchString, +4 test files). NO new Go deps (stdlib net/http). Oracle: the 56-case `FetchUtils.test.js` ported (fetchJson/Headers/fetchStream/fetchNothing/RequestFailedError/fetchString/fetchRedirect) + the 10 agent cases (success, non-routable→ConnectTimeoutError→FetchError, retries-after-delay with ≥1500ms pin, no-stray-reconnect, https CA, untrusted→DEPTH_ZERO_SELF_SIGNED_CERT, https non-routable, bad CA PEM, non-positive connectTimeout) + over-timeout warn (120s seam made a var) + edge branches (FetchError surface, non-string header stringify, JSON-body marshal error, expired-cert→ERR_CERT_EXPIRED). Coverage 94.9%.
  **Go-ism seams (narrowest faithful equivalent + test pin):**
  (a) Node `AbortController`/`signal` → Go `context.Context` (cancel == abort); `errors.Is(..., context.Canceled)` NOT used because oerror deliberately does not Unwrap non-OError causes into the stack (pinned in oerror tests) — abort tests assert the transport `context canceled` text instead.
  (b) Node agent (http.Agent with connect-timeout retry) → `*http.Client` + custom `DialContext`/`DialTLSContext` (DialTLSContext is REQUIRED: a DialContext that already did TLS would double-handshake and the server would answer plaintext → "server gave HTTP response to HTTPS client"). Retry loop = up to 3 attempts, per-attempt ctx timeout, retry interval, last-error-wins, timeout→ConnectTimeoutError.
  (c) Node request-body `stream.destroyed` → a `bodyPipe` (io.Pipe + source-Closer tracking): destroy-on-error, destroy-before-transfer (source.Close unblocks the copy and aborts the request via CloseWithError — a clean EOF would only end the body, NOT abort), destroy-if-not-consumed after settle (raw early responder needed: the net/http test server drains the body before flushing early responses, which blocks on unbounded bodies — a Node/Go server-stack diverge, see §Divergences).
  (d) `setLogger({warn})` 120s over-timeout warn → `SetLogger(WarnFunc)` + `RequestWarnTimeout` var (test-overridable) firing with `{url,method,overTimeoutMs,stack}` + the exact Node message "Fetch request did not complete within 120 seconds".
  (e) node-fetch `FetchError` wrapping: connect timeout → FetchError"request to <url> failed, reason: connect timeout"; TLS trust failure → FetchError{Code: DEPTH_ZERO_SELF_SIGNED_CERT} (untrusted/self-signed) / ERR_CERT_EXPIRED. Mapped BEFORE the oerror.Tag wrap (Tag breaks the Unwrap chain for non-OError causes) and inside performRequest so the raw transport error chain is still visible. `errors.As` targets must be VALUE types for x509.CertificateInvalidError / x509.UnknownAuthorityError (they are value-receiver types held as values in the chain — pointer targets do NOT match).
  (f) `parseHeaders` drops null-valued headers (Node stringifies `undefined` → dropped); non-string values stringified (bool/int/float). RequestFailedError = `request failed` message, body-in-info only for 400/409/413/422.
  (g) fetchRedirect returns the RAW Location header (Node parity); the test server issues absolute Locations (Node's server resolves relative→absolute; Go's does not).
  **TEST-ONLY infra pins (not shipped behaviour):** /hang handler blocks on `r.Context().Done()` (not `select{}`) with a 30s self-timeout so Server.Close can't hang; cleanup uses CloseClientConnections + non-blocking Close for live /hang conns (client abandoned mid-request); waitForRequest replaced by a path-polling waitUntilRequest (stale buffered tokens from earlier subtests caused a cancel-before-send race); rawEarlyResponder (raw TCP) mirrors express /json/ignore-request (responds without reading the body).
- (LIB-08) **accesstoken = 1:1 port of `libraries/access-token-encryptor`** (AccessTokenEncryptor.js) into `go/libraries/accesstoken` (accesstoken.go). NO new Go deps (stdlib crypto/{aes,cipher,hkdf,sha512,rand}). Oracle: the Node 27-case suite — constructor validation (all 6 error messages verbatim, including the UTF-16-code-unit "too short" check), encrypt round-trip (label:saltHex16:ctB64:ivHex16 format regex, fresh salt+iv so two encryptions differ), decrypt 2023 golden token → {"hello":"world"}, unknown-2015/2016/2019 labels (v1/v2 schemes not implementable → "unknown access-token-encryptor label <x>"), corrupt-ciphertext/invalid-base64 → "error decrypting token", invalid label/key. Coverage 95.4%.
  **Port internals:** scheme dispatch by first `:`-separated label (Node `split(':',1)`); v3 keyFn = HKDF-SHA512(password_utf8, salt, info="", L=32) — RFC 5869, byte-identical across stacks (Go `hkdf.Key(sha512.New, secret, salt, "", 32)` = Node `crypto.hkdf('sha512', pw, salt, '', 32)`); AES-256-CTR (Node `createCipheriv/Decipheriv`) — CTR has no auth tag so "bad ciphertext" is caught at the JSON.parse step (Node `error decrypting token`), reproduced by the json.Unmarshal branch. Password UTF-16 length via `utf16.Encode` (JS `.length` ≠ byte count). `labelsInOrder` + Config.PasswordOrder restore Node's Object.keys insertion order (Go map iteration is random) so multi-scheme validation/unknown-scheme reports are deterministic and Node-ordered; unlisted config keys are appended sorted.
  **THE 2026-09-21 RED-FIX:** `TestDecryptGolden2023` was FAILING not due to a crypto bug — the Go TEST password was `strings.Repeat("4", 46)` but the Node oracle password is `'4'×38` (counted in `AccessTokenEncryptorTests.js` `this.settings.cipherPasswords`). HKDF is deterministic, so a 46-char password derives a different key and the golden ciphertext decrypts to garbage → `error decrypting token`. Corrected all 6 test passwords to 38. **Lesson: when a 1:1 crypto port's golden test fails, verify the TEST's inputs (salt/iv/password/label) against the Node source byte-for-byte BEFORE suspecting the algorithm — the algorithm was already byte-parity.** (Cross-checked by running the live Node `test:unit` 27/27 passing, and by a direct node one-liner HKDF+CTR decrypt of the exact golden token with the 38-char password.)
- (owner 2026-09-21, task (b)) **accesstoken `Config.RelaxedLabels` extension** (accesstoken.go + accesstoken_relaxed_test.go): the Overleaf **web** service's per-provider token ciphers (`features/{sitesettings,webdav,mndeley}/cipher.go`) implement the SAME v3 scheme (HKDF-SHA512 + AES-256-CTR, the identical `label:saltHex:ctB64:ivHex` token) but treat the label as an OPAQUE prefix with NO version-suffix gate, so an operator-configured multi-dash label (e.g. `OL-CEP-2042-v3`) works in web but the strict `AccessTokenEncryptor` constructor (Node line 108 `version = cipherLabel.split('-')[1]`) rejects it (`unknown version`). `RelaxedLabels:true` waives ONLY that label-version gate; every other validation (non-empty, no-colon, password present + `≥16` UTF-16 units) and the SAME v3 crypto/wire are unchanged. This lets the web ciphers reuse this one crypto implementation (dedupe ~150 inline HKDF+AES lines) instead of re-implementing it. **Owner rule applied: library extension allowed only if documented + tested + oracle-pinned** — the strict Node-oracle path (default) is byte-for-byte unchanged and all 18 existing Node tests still pass; `accesstoken_relaxed_test.go` pins strict-rejects-multi-dash / relaxed-accepts / relaxed-keeps-other-validation / relaxed-shares-crypto-with-strict. **KEY DISCOVERY:** web's 5 `cipher.go` files are NOT one scheme — the CTR/HKDF family (sitesettings core + webdav + mendeley, the latters already thin delegates to sitesettings) maps here; the **GCM** family (dropbox AES-256-GCM+sha256, llmsettings AES-GCM+scrypt) is a DIFFERENT scheme and is intentionally NOT mapped. (The web→accesstoken cipher reuse itself is the follow-on slice.)
- (LIB-10) **rediswrapper = 1:1 port of `libraries/redis-wrapper`** (index.js 239LoC + RedisLocker.js + RedisWebLocker.js + Errors.js) into `go/libraries/rediswrapper` (driver.go / client.go / health.go / locker.go / weblocker.go / errors.go + 6 test files). NO new Go deps (stdlib only + oerror). **Oracle: the Node unit suite is fully driver-mocked (`sandboxed-module` stubs ioredis) — 14/14 passing — so the Go port pins the SAME contract against an injected `Driver` seam; no live redis needed for unit tests** (the Node `test/scripts/standalone.js|cluster.js` are manual live scripts, not suite members). Coverage 95.3% (52 test fns).
- (LIB-11) **mongoutils = 1:1 port of `libraries/mongo-utils`** (ObjectId.js + testutils.js + batchedUpdate.js) into `go/libraries/mongoutils` (objectid.go / testutils.go / options.go / batchedupdate.go + test files). **mongowrapper = façade over `libraries/mongoose-wrapper`** (a 6-line mongoose re-export → stable import point over `go.mongodb.org/mongo-driver`). NO new Go deps (mongo-driver + stdlib already in go.mod). **No seam needed: the functions take `*mongo.Collection` directly (Go pointers, not JS objects), and the suite runs against a real local mongod** (batchedUpdate live walk in both directions, cleanup/drop live, single-flight, options refresh). Deterministic ObjectID edges are pinned with fixed hex timestamps so the walk is reproducible. batchedUpdate pins Node's ascending/descending ObjectID time-window walk, single-flight, options refresh (env `BATCH_*`), and the `ID_EDGE_FUTURE` sentinel. Coverage: mongoutils 88.0%, mongowrapper 92.5%.
- (LIB-12) **persistors = 1:1 port of `libraries/object-persistor`** into `go/libraries/persistors`. Files: `errors.go` (the 7 typed error classes + `asPersistorError` instanceof checks), `helper.go` (PersistorHelper: `wrapError` classification, `Observer` metrics/hash, md5 helpers, `verifyMd5`), `projectkey.go` (ProjectKey format/pad), `ssec.go` (SSE-C key options), `abstract.go` (`Persistor` contract + `BasePersistor` NotImplementedError defaults), `s3_seam.go` / `gcs_seam.go` (narrow SDK seams + `S3Error`/`GCSStatusError` error-shape adapters), `s3persistor.go` / `gcpersistor.go` / `fpersistor.go` (the three backends), `migration.go` (MigrationPersistor primary→fallback + copy-on-miss), `perproject.go` (PerProjectEncryptedS3Persistor + DEK/KEK lifecycle + HKDF), `factory.go` (PersistorFactory + `ObjectPersistor`). **No new Go deps** — `go.mongodb.org`/AWS/GCS SDKs are NOT imported: the Node unit oracle stubs the SDK client (`S3ClientMock.js`, sinon `@google-cloud/storage`), so the Go suite injects FAKE clients implementing the `S3Client`/`GCSStorage` seams (same mock-based approach as LIB-10's `Driver` seam). A real runtime adapter (e.g. the repo's existing S3 gateway `ollitex/go/s3x`) plugs into the seam later without touching the persisted logic. `Logger`/`Metrics` are `Null*`-defaulted NARROW interfaces (`LoggerAPI`/`MetricsAPI`) — they become the real ologger/LIB-13 + ometrics/LIB-14 once those land (no premature coupling). **Go-ism fix pinned:** `autoGunzip` must use `compress/gzip` (Node `zlib.createGunzip`), NOT `compress/zlib` (zlib wrapper format) — an initial wrong choice was caught by the gunzip oracle test. Per-KEK/DEK SSE-C paths, `ifNoneMatch:'*'`→generation-0 / AlreadyWritten mapping, `deletedBucketSuffix`, `unlockBeforeDelete`, `deleteConcurrency`, copy-on-miss and the S3SSEC `no kek matched`/`kek is not 32 bytes long` strings are all oracle-pinned. Coverage: **88.6%** (above the 85% gate).
  - Known divergence: Node `ObjectId` is the 12-byte BSON id from `mongodb`; Go uses `primitive.ObjectID` (same 12 bytes, hex + 32-bit-second timestamp). `ObjectIdFromTimestamp` parity is exact at second resolution (sub-second ms truncate, as in Node's driver).
  **Seam (the Node value IS the driver adapter):** `Driver` interface (SetEx NX/EX, Exists, Eval, Exec→RAW ioredis rows, FlushAll, Host) + `Configure(opts, clusterConfig)` constructor seam (Node `new Redis(opts)` / `new Redis.Cluster(nodes, opts)`); `CreateClient(opts, ctor)` mirrors index.js 1:1 — key_schema stripped, `retry_max_delay ??= 5000`, sentinel → exact `@overleaf/redis-wrapper: redis-sentinel is no longer supported`, cluster dispatch (client.ClusterConfig = nodes, `cluster` deleted from driver opts) vs single-instance.
  **healthCheck** 1:1: token `host=…:pid=…:random=<8hex>:time=<ms>:count=N` (process-unique), key/value `_redis-wrapper:healthCheck{Key,Value}:{token}`, SET EX 60 (non-NX) → multi GET+DEL; error classes `RedisHealthCheckTimedOut('timeout')` (2000ms `HealthCheckTimeout` var seam), `RedisHealthCheckWriteError('write errored'|'write failed')`, `RedisHealthCheckVerifyError('read/delete errored'|'read failed'|'delete failed')` with the Node stage/context fields (writeAck / roundTrippedHealthCheckValue / deleteAck / uniqueToken) — all OError-typed via Errors.js hierarchy (name/message/cause pinned).
  **RedisLocker** 1:1: TTL validation `30 <= ttl < 1000` → exact `redis lock TTL must be at least 30s and below 1000s` (Node "wrong type" case unrepresentable in the int-typed Go API — documented); signed tokens `locked:host…:count…`; SET NX EX; overlong-SET auto-release (`MaxRedisRequestLength` var seam, Node MAX_REDIS_REQUEST_LENGTH=5000ms); poll 50ms→1s backoff cap, `MaxLockWaitTime` 10s → `WrapTimeoutError(Error('Timeout'), id)` (injectable like Node's wrapTimeoutError); CheckLock exists→free?; Extend/Release via the two Lua scripts byte-for-byte (extend arg `lockValue, lockTTLSeconds`; unlock result!==1 → `tried to release timed out lock` / `tried to extend a lock we no longer hold`); metrics `prefix-not-blocking|-blocking|-extend-*` + logger seams (Null* defaults).
  **RedisWebLocker** 1:1: per-key FIFO lock queue = Node `async.queue(concurrency 1)` (LOCK_QUEUES is module-scope → package `lockQueues`; drain removes the entry; `LockQueuesSize` = `_lockQueuesSize`); task continuation = `queue.next()` invoked BEFORE the caller resolves (pin-tested by real store contention: two concurrent RunWithLock on one key serialize, neither interleaves); `_getLockByPolling` FIXED-interval (no backoff, unlike RedisLocker), attempts gauge `lock.<ns>.get.success.tries` / `lock.<ns>.get.failed`; **Node TTL quirk pinned 1:1: `EX REDIS_LOCK_EXPIRY*1000` (EX is seconds → the redis-side TTL is ×1000; the client-side watchdog fires after REDIS_LOCK_EXPIRY seconds: `lock.<ns>.exceeded_lock_timeout` inc + debug log)**; RunWithLock = Node promisified contract: timer `lock.<ns>` → acquire → runner(values, runnerErr) → release (Node `error1 || error2`: runner error wins; release error only surfaces on runner success) → `slow execution during lock` debug over `slow_execution_threshold`; `slowExecutionError` sentinel in the log info.
  **cleanupTestRedis/ensureTestRedis** 1:1: `Refusing to clear Redis instance '<host>' in environment '<env>'` (host must be `redis_test` AND NODE_ENV=test) → FLUSHALL.
  **TEST-ONLY infra:** fakeDriver = scripted responses (repeat-last-element) OR real in-memory store semantics (SET NX exclusivity, unlock/extend CAS over the held value) — this is what makes the serialization/contention pins genuine; echoSetDriver for the healthCheck round-trip; recording metrics/logger doubles.
- (LIB-13) **ologger = 1:1 port of `libraries/logger`** (logging-manager.js + serializers.js + log-level-checker.js + gcp-manager.js) into `go/libraries/ologger` (ologger.go / manager.go / seams.go / real.go / serializers.go / log_level_checker.go / gcp_manager.go + 7 test files). **No new Go deps** (stdlib + `ollitex/go/libraries/oerror` for `GetFullStack` in the err serializer). **Oracle: the Node 31-test suite is fully bunyan-sandboxed (`sandboxed-module` stubs `bunyan`, `@overleaf/fetch-utils`, `@overleaf/validation-tools`, `./log-level-checker`) — so the Go port injects a narrow `Bunyan` (createLogger+RingBuffer) / `Logger` / `CheckerFactories` seam and the Go tests drive a STUB bunyan exactly like the Node sandbox** (`NewJSONBunyan` is the real standalone default: a JSON-line sink with level filtering — a Go addition the Node suite never exercises, tested independently). Files: `Bunyan`/`Logger`/`StreamConfig`/`RingBuffer`/`Checker` seams (`seams.go`); `LoggingManager` (initialize/addSerializer/debug/info/error/err/warn/fatal/exit/warning-handler/ring-buffer/level-checker wiring) (`manager.go`); the four base serializers + `Req` model + `getRawReqInput` + `getRemoteIp` (`serializers.go`); `FileLogLevelChecker`/`GCEMetadataLogLevelChecker` + base interval loop (`log_level_checker.go`); `ConvertLogEntry` GCP converter + `omit` + label helpers (`gcp_manager.go`). **Go-ism decisions pinned by tests:** (1) `attributes`/`message` are `any` (not `map`/`string`) because the Node "bunyan logging" oracle passes a single ARRAY as the sole arg with `message` undefined and expects it forwarded verbatim — so the manager passes the first arg through and `error()` only injects `logBuffer` when `attributes` is a `map[string]any`; (2) **req-serializer lockdown**: Node's `getRawReqInput` reads the patched-express symbol-keyed raw accessors instead of the throwing `req.params` getter — Go models this as `Req.LockdownInstalled` + `Req.RawParams` (the safe source) vs `Req.Params` (poison, MUST NOT be read); the oracle test pins ids coming from `RawParams` while `Params` holds a wrong value; (3) **real-logger level filter corrected** to bunyan semantics (`suppressed when level < configuredLevel`, i.e. `fatal` always emits, `debug` suppressed at `info` level) — an initial inverted guard would have dropped `fatal`; (4) GCE checker fetches the exact URI `http://metadata.google.internal/computeMetadata/v1/project/attributes/<name>-setLogLevelEndTime` with header `Metadata-Flavor: Google` (name from `Logger.Name()`); (5) `parseInt(s)||0` semantics (leading signed digits; NaN→0) for the ring buffer + tracing end-time; (6) `setLogger(this)` on both `@overleaf/fetch-utils` and `@overleaf/validation-tools` is an injectable `Warn`-callback seam (default no-op; the host wires the real `fetchutils.SetLogger`). The GKE `LoggingBunyan`/`@google-cloud/logging-bunyan` runtime adapters are seams (flagged `StreamConfig.GKE/GCE`), not imported — same mock-based approach as LIB-10/LIB-12. **Go gotcha pinned:** `if x != T{} {` (composite literal immediately before an if-block brace) does not parse — the test asserts the `{err}` identity via a type-assertion instead. `Logger`/`Metrics` here become the real logger for the persistors `NullLogger` default. Coverage: **94.2%** (above the 85% gate; all 31 Node oracle tests have a green Go counterpart).
- (LIB-14) **ometrics = 1:1 port of `libraries/metrics`** into `go/libraries/ometrics`. **No new Go deps** — a faithful prometheus-flavoured registry is built OVER THE STDLIB (no `prometheus` client imported), mirroring `prom-client` semantics the oracle pins: `Registry` (cached-by-name `Metric`s + `getMetricsAsJSON` + default `host`/`app` labels via `SetDefaultLabels` + TTL/sweep), `Metric` `Counter`/`Gauge`/`Summary`(quantiles + `_sum` + `_count`)/`Histogram`(CUMULATIVE `le` buckets + `+Inf` + `_sum` + `_count`), nearest-rank (ceil) quantiles, and `buildPromKey` (non-alphanumeric → `_`) so `show.me the $!!` → `show_me_the____`. The index.js API (`inc`/`count`/`summary`/`timing`/`histogram`/`gauge`/`globalGauge`/`Timer`) is a direct port (`metrics.go`). **Node-platform specifics are behind NARROW injector seams (the documented Go divergence for this lib)** — the Node oracle reads platform internals that don't exist in Go: `SocketsTracker` (Node `http/https globalAgent.sockets`) for `open_sockets`, `MemorySource`+`ForceGC` (Node `process.memoryUsage`/`global.gc`) for `memory`, `ActiveHandles`+`ReadProcNetTcp` (Node `process._getActiveHandles`/`/proc/net/tcp`) for `leaked_sockets`, and `Recorder`/`RegisterDestructor`/`NowMS` (injectable clock) for the http/event_loop collectors — the LOGIC is 1:1, only the platform data source is a test seam (same approach as LIB-10/LIB-12/LIB-13). The three same-named Node `monitor` exports are renamed `HTTPMonitor`/`EventLoopMonitor`/`MongoMonitor`. `initialize.js` (`setDefaultLabels({app,host})` + `process_startup` + default `process_*` metrics), `open_sockets` (seen-host gauges + legacy `${status}_connections.${scheme}.${host}` names), `memory` (cpu-bucket/gc-interval logic), `leaked_sockets` (redact/flatten/isOldSocket/`/proc/net/tcp` decode), `uv_threadpool_size` and `index.injectMetricsRoute` (a thin `MetricsHandler`) are all ported. The mongodb `poolSize` `collect` hook resets all four pool gauges + re-runs registered collect fns (mirroring prom-client during-serialization collect); the `timer_` prefix on `timing()` and `histogram_` on `histogram()` are preserved (`event-loop-millsec` → `timer_event_loop_millsec`). **Oracle: the Node 37-test suite (15 unit http/event_loop/mongodb + 22 acceptance incl. Timer `beforeEach` accumulation + open_sockets) is green, and every assertion has a green Go counterpart** — incl. the acceptance-ordered Timer (9 then 18 samples; quantile oracle is scale-invariant) and the open_sockets seen-host 2/1/0 lifecycle driven via the `SocketsTracker` seam. Coverage: **95.9%** (above the 85% gate).
- (LIB-15 Phase A) **otc = 1:1 port of `libraries/overleaf-editor-core` — Phase A (OT core) only**, into `go/libraries/otc`. Multi-package Node lib split into focused Go files, Phase A = the operational-transformation text-operation primitives that `internal/opmodel` / the document store build on. **No new Go deps** (stdlib + `ollitex/go/libraries/oerror` unused — self-contained).
  - **Files:** `otc.go` (pkg doc + phase map), `errors.go` (the OT error family), `util.go` (`ContainsNonBmpChars`), `range.go` (Range: start/end/contains/overlaps/touches/subtract/canMerge/merge/moveBy/extendBy/shrinkBy/insertAt/splitAt/intersect), `tracking.go` (TrackingProps + ClearTrackingProps behind a `TrackingDirective` interface; `ts` is a `time.Time` rendered as ISO-8601 ms), `scan_op.go` (ScanOp + RetainOp/InsertOp/RemoveOp + `isRetain/isInsert/isRemove` + raw codec), `comment.go` (Comment), `comment_list.go` (CommentList, id-keyed + insertion-ordered), `tracked_change.go` (TrackedChange), `tracked_change_list.go` (TrackedChangeList + apply*), `string_file_data.go` (StringFileData), `text_operation.go` (the core), `raw_conv.go` (tolerant raw decoding), + 4 test files (`otc_helpers_test.go` random gens + raw-compare, `range_test.go`, `text_operation_test.go` + `text_operation_ops_test.go` fuzz, `coverage_test.go`).
  - **Core is a faithful 1:1 of `text_operation.js`** (itself the OT.js 2013 port): `retain/insert/remove` builders with the "insert-before-remove" ordering + merge, `equals/isNoop/toString/toJSON/fromJSON`, `apply` (mutates `StringFileData`: content + comments + trackedChanges), `applyToLength`, `invert` (the tracked-change/comment segment restoration incl. `calculateTrackingCommentSegments`), `canBeComposedWith(ForUndo)`, `compose`, and `transform` (the OT heart). `MAX_STRING_LENGTH = 3*1024^2`.
  - **Error family = separate Go structs embedding `*UnprocessableError`** so the oracle can `errors.As`/assert each: `*ApplyError` ("The operation's base length must be equal to the string's length.", "Operation can't retain more chars than are left in the string.", "The operation didn't operate on the whole string."), `*InvalidInsertionError` ("inserted text contains non BMP characters"), `*TooLongError` ("resulting string would be too long"), `*UnprocessableError` ("unknown operation: …", "Both operations have to have the same base length", "The two operations aren't compatible", "Trying to compose…").
  - **Go-ism decisions (narrowest faithful equivalent + test pin):** (1) Node throws `OError`/`Error` on Range invariant violations (negative pos/length, shrink-too-much, split/insert-out-of-range) → Go **panics** with the same messages (the oracle only asserts `.to.throw()`, message text is not checked for these; OT-specific errors DO surface as return-value errors above). (2) Node `opts` object + `instanceof` → Go `TrackingDirective` interface (`TrackingProps` value / `ClearTrackingProps` value) + builder `RetainBuilderOpts{Tracking}` / `InsertBuilderOpts{Tracking,CommentIds}`; `ClearTrackingProps` is a zero-size marker value. (3) Node `new Date()` ms → Go `time.Time` rendered via `isoDate` (`.000Z` ms precision) and parsed with `time.Parse(RFC3339Nano)`; merge keeps the LOWER ts (oracle-pinned "chooses lower tracked change timestamp"). (4) Node `ToRaw()` is typed Go values (`[]map[string]any` / `[]map[string]int`), but JSON-decoded raw is `[]any` — `raw_conv.go` (`toAnySlice`/`asRawMap`/`asRawObjects`/`asRawRanges`) tolerates BOTH so `FromRawStringFileData(file.ToRaw())` round-trips exactly like Node's `StringFileData.fromRaw(toRaw())` (this was the crux bug: comments/trackedChanges silently dropped in the in-memory round-trip). (5) Node's `MergeWith` mutates in place → Go ops are immutable and `MergeWith` RETURNS the merged op (the builder replaces the stored last-op); ScanOp methods are all value receivers so the value implements the interface.
  - **Fuzz = the Node randomised oracles, seed-pinned:** `otc_helpers_test.go` mirrors `test/unit/support/random.js` + `random_text_operation.js` (same distributions, fixed `rand.NewSource` seeds for reproducibility). `TestApplyRandomised` (apply ⇒ targetLength), `TestInvertRandomised` (invert ⇒ o.base==p.target & round-trip toRaw), `TestJSONRoundTripRandomised`, `TestComposeRandomised` (compose ⇒ apply(A)∘B == compose ⇒ same toRaw), `TestComposeAssociativityRandomised` ((A∘B)∘C == A∘(B∘C) modulo timestamps, per the Node "associativity does not handle timestamps" note), `TestTransformRandomised` (transform ⇒ compose(A,B') == compose(B,A') ⇒ same toRaw). 500 trials each.
  - **Oracle: the Node `text_operation.test.js` (1149-line) + `range.test.js` + `tracked_change*` suites are green (657 passing across the package), and every Phase-A assertion has a green Go counterpart** — including the 6 deterministic invert cases, 6 compose cases (incl. "op2 clears insert/delete tracking" + the comment-range both-ops case), the 8 transform cases (incl. comment preservation/extension + tracked-change splitting/insertion), the non-BMP reject, and the timestamp-associativity divergence. Coverage: **91.9%** (above the 85% gate).
  - **Phase A scope boundaries (excluded → Phase B/C):** the file-tree `Operation` family (`NoOperation`/`AddFileOperation`/`MoveFileOperation`/`EditFileOperation`/`SetFileMetadataOperation`/`EditOperationTransformer`), `Operation` (op/index.js), `rebase.js`, `Change`, `Snapshot`, the OT client/history, `file_map`, `blob_store`, `schemas`. These depend on the file-tree op family + `StringFileData.edit` wiring and are the next commit(s).

- (LIB-15 Phase B slice 1) **otc — file-tree value model**, 1:1 into `go/libraries/otc` (Phase A's self-contained package). **No new Go deps** (stdlib `path`/`os`/`unicode/utf8`+`utf16`/`time`/`regexp` + `ollitex` oerror already imported). Files + oracle:
  - `text_file_defaults.go`: `DefaultTextExtensions`(42), `DefaultRootDocExtensions`(4: tex/Rtex/ltx/Rnw — Node preserves the mixed case), `DefaultEditableFilenames`(4). Pinned for exact contents.
  - `author.go`/`author_list.go`: `Author{ID,Email,Name}` + `NewAuthor`/`AuthorFromRaw`(nil→nil)/`ToRaw`/getters; `AssertAuthorsV1`(all-number OR all-`*Author`, nils disregarded) and `AssertAuthorsV2`(24-hex v2 id, `check.assert.maybe` ⇒ `nil` allowed). Node check-types dynamic asserts → Go static types for the scalar guards; the list-level validators are explicit functions (panicking a typed `*typeError`, mirroring the JS `TypeError`).
  - `label.go`: `Label{Text, AuthorID *int64 (nil=null), Timestamp time.Time, Version int64}` + `NewLabel`/`LabelFromRaw`(`new Date(raw.timestamp)`) /`ToRaw`(ISO ms, Node `toISOString`)/getters. Oracle: anonymous-author (null) case.
  - `file_metadata.go`: `DocumentMetadataKeys`=`[main,mainBibliography]`, `IsDocumentMetadata`(every key is a doc key), `HasDocumentMetadataFlag`(truthy), `WithDocumentMetadataFlag`(set/clear one flag, keep the other, drop non-flag keys, cleared flag left out ⇒ empty map).
  - `safe_pathname.go`: `Clean`/`CleanDebug`/`IsClean`/`IsCleanDebug` — the exact `cleanDebug` pipeline (normalize → backslash→/ → collapse `//` → no leading `/` → no trailing `/` → no leading/trailing ` ` → empty→`_` → per-part `cleanPart` → `BLOCKED_FILE_RX` `@` prefix) **with the comma-joined reason string pinned byte-for-byte**. `cleanPart` = `BAD_CHAR_RX`→`_` + `BAD_FILE_RX`(`.`→`_`,`..`→`__`,leading/trailing-space→`_`)。 **Go-ism:** `posibNormalize` is a 1:1 reproduction of `path-browserify`/Node posix `path.normalize` (verified against Node `path.posix.normalize` on every test input: `./`-drop, `..` pop / above-root keep, discard `..` at an absolute root, trailing slash preserved when non-empty). Non-BMP chars (surrogate pairs) are in the `BAD_CHAR` set (Node `\uD800-\uDFFF`). `MAX_PATH=1024` compared in RUNES (Node `.length`=UTF-16 units; identical on the ASCII oracle).
  - `file_type_detector.go`: `FileTypeConfig`{TextExtensions,EditableFilenames,MaxDocLength}, `DetectedType`{Kind,Content}; `IsTextFilename`(basename in editable list OR `.ext` in extensions), `NeedsContent`(file→false; not-doc-and-not-text-name→false; byteLen>3×max→false), `IsEditableString`(UTF-16 units < maxDocLength, no NUL, no non-BMP), `DetectBuffer`(`needsContent`→`utf8.Valid`→`IsEditableString`), `DetectFile`(`os.Stat`+`os.ReadFile` then `DetectBuffer`). **Go-ism:** Node `utf-8-validate`→Go `utf8.Valid`; Node `content.length`→`len(utf16.Encode([]rune(s)))`; Node `path.browserify` basename/extname→Go `path.Base`/`path.Ext`(lower-cased); Node `TypeError` on a missing/bad config→the `nil *FileTypeConfig` guard panics `"fileTypeDetector: bad config"` (the partial-config sub-cases Node tests are unrepresentable under Go's typed struct — documented, the `nil`-config case is the faithful equivalent and is test-pinned).
  - **Oracle: the Node `label`/`file_metadata`/`safe_pathname`(all 40+ expectClean rows incl. exact reasons + idempotency)/`file_type_detector`(both entry points, all size/UTF-8/BOM/lone-surrogate/non-BMP rows) suites are green (within the package's 657 passing), and every row has a green Go counterpart.** Coverage: **92.9%** cumulative (above the 85% gate).
  - **Phase B scope still open:** `File`+`FileData` hierarchy (hash/binary/hollow/lazy) + `Blob`/`BlobStore`, `FileMap`, the `Operation` family (`NoOperation`/`AddFileOperation`/`MoveFileOperation`/`EditFileOperation`/`SetFileMetadataOperation`/`EditOperation`/`EditOperationBuilder`/`EditOperationTransformer`), `Operation`(index), `rebase`. Then Phase C: `Change`/`Snapshot`/`History`/`Chunk`/`blob_store`/`schemas`/`ot_client`/`diff_match_patch`/`root_doc`/`origin`.

- (LIB-15 Phase B slice 2) **otc — Blob + blob_utils**, 1:1 into `go/libraries/otc`.
  - `blob.go`: `Blob{Hash, ByteLength int64, StringLength *int64 (nil=undefined)}` + `NewBlob`(validates the 40-hex `HEX_HASH_RX`, non-negative byteLength) /`BlobFromRaw`(nil→nil)/`ToRaw`/getters; constants `HexHashRxString`(40-hex), `MaxEditableByteLengthBound` = `3*MaxStringLength`(a BMP char is at most 3 UTF-8 bytes), `EmptyHash`(git empty-blob `e69de29b…`); `BlobNotFoundError`("blob <hash> not found" + hash).
  - `blob_utils.go`: `BlobHashFromString`/`BlobHashFromBuffer`/`BlobHashFromStream`/`BlobHashFromFile` = git blob hash `sha1("blob "+byteLength+"\0"+content)`, where the header counts BYTES (not chars) — stdlib `crypto/sha1` (Node `node:crypto`). This is byte-for-byte git's `hash-object` (the hash history stores is the sha on a git repo / GitHub), so it's the acceptance pin. `GetStringLengthOfBuffer` (utf8.Valid ⇒ UTF-16 units ≤ MaxStringLength, no NUL, no non-BMP) /`GetStringLengthOfFile` (skips the read for > MaxEditableByteLengthBound) /`BlobForFile` (hash + byteLength + stringLength when editable). `utf-8-validate`→`utf8.Valid`; Node `content.length`→`utf16.Encode`; the `TypeError` on a bad byteLength → a `*typeError`. `blobHashFromStream` non-integer → the Go typed int can't be NaN; the negative guard returns a `*typeError` (mirroring the Node `TypeError`).
  - **Oracle: the Node `blob_utils` suite (32 passing, incl. the exact `git hash-object` shas and the GitHub-reported exported-project shas) is green and every row has a green Go counterpart.** The git-blob-hash pin is the high-value correctness anchor for pushing a history snapshot to GitHub without re-uploading blobs. Coverage: **92.6%** cumulative (above the 85% gate).

- (LIB-15 Phase B slice 3) **otc — EditOperation family + BlobStore seam**, 1:1 into `go/libraries/otc`. This is the edit-operations layer the File/FileData/Operation model builds on; it is a self-contained unit (no `File`/`Snapshot`/`Change` dependency), so it commits standalone.
  - `edit_operation.go`: the `EditOperation` interface (`ToJSON`/`Apply`/`ApplyToLength`/`Invert`/`CanBeComposedWith`/`CanBeComposedWithForUndo`/`Compose`) + `opTypeName` (Node class-name labels for the “Trying to compose X with Y” messages) + `asTextOp`/`mustJSON`. Node methods that `throw` are returned as `error` (Phase A convention for the OT error family) — documented.
  - `edit_operation_impls.go`: `textEdit` (adapts Phase A `*TextOperation` to the interface — Node's `TextOperation extends EditOperation`); `AddCommentOp` (`NewAddCommentOp` rejects any EMPTY range: “AddCommentOperation can't be built with empty ranges”; an empty ranges ARRAY is allowed, matching Node's per-range `range.isEmpty()` check; `toJSON` omits `resolved` when false; `invert` → the prior comment or a `DeleteCommentOp`; `compose` → delete/add-win / set-adopts-resolved, else the message-pinned error); `DeleteCommentOp` (`toJSON {deleteComment}`; `invert` → prior comment re-add or `EditNoOp`); `SetCommentStateOp` (`toJSON {resolved,commentId}`; `apply` no-ops on a missing comment; `invert` → prior resolved state or `EditNoOp`; `compose` → same-id set/delete win); `EditNoOp` (`toJSON {noOp:true}`); `rangesToRaw`.
  - `edit_operation_builder.go`: `FromJSONEditOperation`/`IsValidEditOperationRaw` — key-presence dispatch in Node's order (textOperation / commentId+ranges[] / deleteComment / commentId+resolved:bool / noOp) with the “Unsupported operation in EditOperationBuilder.fromJSON” error.
  - `edit_operation_transformer.go`: `TransformEditOps` (the 10-case matrix: text×{text,delete,set,add}, add×{add,delete,set}, delete×{delete,set}, set×set, incl. the symmetric branch and the U+FFAE “Transform not implemented for A￮B” fallback) + `TransformEditOpsMultiple`. The (text,add) case applies the text op to the comment (Phase A `Comment.ApplyTextOperation`) and re-homes the ranges — oracle-pinned (insert-6 shifts `[10,3]`→`[16,3]`; a covering delete empties the ranges; a partial delete shrinks `[10,3]`→`[8,1]`).
  - `blob_store.go`: the `BlobStore` interface (GetBlob/GetString/GetObject/PutString/PutObject — Node's async fns as (value,error)) + `BaseBlobStore` supplying the `BlobStoreBase` defaults (GetBlob→nil, GetString→'' for empty hash else fetch, GetObject→JSON.parse or {}).
  - `string_file_data.go`: + `Edit(op EditOperation) error` (Node `StringFileData.edit` = `op.apply(this)`), the one FileData-interface method this slice needs (used by the transformMultiple oracle).
  - **Oracle: the Node `edit_operation` (transform matrix + transformMultiple convergence `yxAB12`) + `add_comment_operation` (8 passing) + `delete_comment_operation` suites are green in the full package (657 passing), and every deterministic assertion has a green Go counterpart.** Note: under isolated mocha a couple of these Node files hit circular-`require` quirks (`instanceof`/constructor undefined) — a harness artifact, not a logic divergence; the authoritative full suite is green. Coverage: **90.0%** (above the 85% gate).

- (LIB-15 Phase B slice 4) **otc — FileData/File/FileMap backbone**, 1:1 into `go/libraries/otc`. The file-tree FILE value model on top of Phase B slices 1-3 (value model + Blob + EditOperation). No new deps beyond `ollitex/go/libraries/oerror` (faithful: Node overleaf-editor-core imports `@overleaf/o-error`).
  - `file_data.go`: the `FileData` interface (15 methods: `ToRaw`/`ToStats`/`GetHash`/`GetRangesHash`/`GetContent`/`IsEditable`/`GetByteLength`/`GetStringLength`/`Edit`/`ToEager`/`ToLazy`/`ToHollow`/`Store`/`GetComments`/`GetTrackedChanges`). Node's null/undefined returns are **nullable pointers** (`*string`/`*int64`/`*bool`; nil = Node undefined/null — e.g. `HashFileData.IsEditable()`→nil, `BinaryFileData.IsEditable()`→`&false`); Node `throw` methods are `error` returns. `fileDataDefaults` is the embedded base (Node FileData base: null accessors + "not implemented" errors); `loadFileData` is a free func taking the interface so `fd.ToEager/ToLazy/ToHollow` reach the *concrete* override (Go embedding cannot re-dispatch through a base method). Registry: `FromRawFileData` (key-presence dispatch hash+byteLength→binary / hash+stringLength→lazy / hash→hash / byteLength→hollowBinary / stringLength→hollowString / content→string), `CreateHollow`, `CreateLazyFromBlobs` (blob.stringLength → LazyString else Binary).
  - `file_data_variants.go`: `HashFileData`/`BinaryFileData`/`HollowStringFileData`/`HollowBinaryFileData` (the four non-lazy variants; hash/binary validate HEX_HASH_RX + non-negative length).
  - `string_file_data.go`: **migrated onto the interface** — accessors now nullable pointers (`GetByteLength() *int64` = UTF-8 bytes, `GetStringLength() *int64` = UTF-16 code units, `GetContent(bool) *string`, `IsEditable() *bool`); `ToStats()` → `map[string]any`; plus `GetComments`/`GetTrackedChanges` (non-nil empty lists), `ToEager`/`ToHollow`/`Store` (putString + optional putObject of the comments+trackedChanges ranges object) and `Edit`. UNPROCESSABLE ERROR gained `Info map[string]any` + `OErrorInfo()`/`SetTagInfo()` so every OT error implements `oerror.InfoProvider` (Node: the OT error family extends OError and exposes `.info`).
  - `lazy_string_file_data.go`: `LazyStringFileData` (hash + rangesHash + stringLength + operations) — `ToRaw`/`fromRaw` (raw text ops; round-trips both `[]any` JSON and `[]map[string]any` in-process shapes), `ToEager` (load content + optional ranges, then applyOperations), `Edit` (applyToLength + queue), `Store` (toEager → store → truncate ops), `ToHollow`, and the **OError.tag error annotations matching the Node oracle** (toEager: `blobHash`/`blobContentLength`/`metadataStringLength`/`totalOperations`/`firstOpBaseLength`/`contentMatchesMetadata`/`contentMatchesFirstOp`; edit: `operationBaseLength`/`totalExistingOperations`; applyOperations: `operationIndex`/`currentContentLength`).
  - `file.go`: `File` (data + metadata) — `FileFromString`/`FileFromHash`/`FileFromRaw`/`FileCreateHollow`/`FileCreateLazyFromBlobs`, `ToRaw`/`ToStats` (+`nMetadata`/`metadataSize`), `Store` (deep-clone of non-empty metadata), `Edit` (NotEditableError when not editable), `Load`/`LoadEager` (answers a concrete `*StringFileData`; NotEditableError otherwise), `Clone`, `GetComments`/`GetTrackedChanges`. `FileEmptyHash` = the git empty-blob hash.
  - `file_map.go`: `FileMap` + the pathname error family (`NonUniquePathnameError`, `BadPathnameError` (truncated + reason), `PathnameConflictError`, `FileNotFoundError`) + `AddFile`/`RemoveFile`/`MoveFile`/`CountFiles`/`GetFile`/`GetPathnames`/`MapValues`/`MapAsync`/`WouldConflict` (strict dir-prefix conflict, case-sensitive, oracle-pinned incl. the `a` vs `a!` sort-order case that must NOT conflict on construct but must conflict on `wouldConflict('a/b')`).
  - **OError annotation design (`tagErr`)**: Node `OError.tag(err,…)` tags IN PLACE (the error remains an instance of ApplyError/TooLongError AND exposes its info via `OError.getFullInfo`). Go equivalent: the OT error types implement `oerror.InfoProvider` and `tagErr` merges the tag info into the error's `Info` map IN PLACE, returning the SAME concrete error — so the oracle's `expect(fn).to.throw(ApplyError)` AND `oerror.GetFullInfo(err)` both hold. Non-OT errors fall back to `oerror.Tag` (wrap). This is the ONLY place otc depends on `ollitex/go/libraries/oerror`.
  - **Divergences (documented)**: the Node `file_map` "does not allow non-safe pathnames" subcase (`addFile('hasOwnProperty')` throws) is NOT pinned in Go — it relies on the JS Object.prototype / lodash `_.has` quirk (a Go map lookup returns the zero value, not a function), so Go correctly succeeds. Same "unrepresentable JS quirk" class as the file_type_detector partial-config note.
  - **Oracle**: the Node `file` + `file_map` + `hash_file_data` + `hollow_string_file_data` + `lazy_string_file_data` + `string_file_data` suites — **56 passing** (incl. all 3 lazy error-annotation tests and the FileMap conflict/case/move/remove/mapAsync oracles). Coverage: **87.6%** (above the 85% gate).

- (LIB-15 Phase C slice 1) **otc — Operation family + Change + Snapshot + rebase + Origin/V2DocVersions**, 1:1 into `go/libraries/otc`. The CONCURRENT-EDIT core: on top of Phase B (file value model), the operations that change a `Snapshot`, and the transforms/rebase that reconcile concurrent edits. New deps: none (stdlib `time`/`regexp` + existing otc + `oerror`).
  - `v2_doc_versions.go`: `V2DocVersions` (id→{pathname,version}) — `NewV2DocVersions`/`FromRaw`(nil→nil)/`ToRaw`(copy)/`Clone`/`ApplyTo`(none→set, existing→merge)/`MoveFile`(re-home or delete, first-match+break). Faithful to node v2_doc_versions.js.
  - `origin.go`: `Origin` base + `RestoreOrigin`/`RestoreFileOrigin`/`RestoreProjectOrigin` behind an `OriginIface` (GetKind/GetHistoryClientId/DropHistoryClientId/ToRaw). `EDITOR_ORIGIN_KIND="editor"` + the 3 restore KINDs. `OriginFromRaw` dispatches by kind+key-presence (restore→version, file-restore→path, project-restore→version, else plain). **Key Go-ism:** `newOriginBase` / `NewOriginWithID(*string)` allows a NIL (absent) historyClientId — Node `new Origin(kind, undefined)` is valid — while the string constructor `NewOrigin(kind,"")` REJECTS the empty string (Node `new Origin(kind,'')` throws). The 3 restore `*FromRaw` construct via the id-pointer path so an id-less origin parses (the raw parsers previously routed through the ""-rejecting string ctor and wrongly threw — fixed). `parseRawTime` accepts ISO-8601 ms / RFC3339(Nano); `toISOString` = `2006-01-02T15:04:05.000Z` (3-fraction-digit, matches `Date.toISOString`). `dropHistoryClientId` keeps subclass fields.
  - `snapshot.go`: `Snapshot` (FileMap + projectVersion + v2DocVersions + timestamp). `SnapshotFromRaw`/`ToRaw` (ISO ms timestamp; projectVersion `^[0-9]+\.[0-9]+$` on set — invalid PANICS a typeError incl. `NewSnapshot` via `SetProjectVersion`); `AddFile`/`MoveFile`(also updates v2 doc versions)/`CountFiles`/`GetFile`/`GetPathnames`; `GetPathnameWithDocFlag(key)` (sorted-first file whose metadata is a doc-only set carrying the flag — oracle-pinned incl. the `importedAt` non-doc exclusion and the not-yet-loaded hash-file case); `EditFile` (throws `EditMissingFileError("can't find file for editing: %s")`, `File.NotEditableError` otherwise); `ApplyAll(changes, strict)` (recoverable = EditMissingFileError|FileNotFoundError ignored unless strict); `FindBlobHashes` (file + ranges hashes); `LoadFiles` (eager, sequential — no concurrency); `Store` (raw files + `timestamp: Date` OBJECT not ISO string, faithful to node); `Clone`. `fileLoadConcurrency=50` mirrors the const.
  - `change.go`: `Change` (operations + timestamp + authors + origin + v2Authors + projectVersion + v2DocVersions). `ChangeFromRaw`/`mustFromRaw` (authors `0→null` bad-data cleanup; `raw.origin && Origin.fromRaw`; `raw.v2DocVersions && …`); `ToRaw` (ISO-millis ts, only-present optional keys); `setOperations` VALIDATES (nil→panic typeError, Node `assert.array`); `FindBlobHashes`/`PushOperation`/`ApplyTo(snap, strict)` (recoverable-ignore vs strict; bumps projectVersion/v2DocVersions/timestamp)/`TransformAfter(other)` (in-place `thisOps[j]=Operation.transform(thisOps[j],otherOps[i])[0]`)/`Clone`/`Store` (`_.uniq(authors)` via `dedupeAuthors` + per-op `store`)/`CanBeComposedWith`.
  - `operation_file_tree.go`: the `Operation` interface (distinct from Phase-B3 `EditOperation`): `ToRaw`/`IsNoOp`/`FindBlobHashes`/`Store`/`ApplyTo`/`CanBeComposedWithForUndo`/`CanBeComposedWith`/`Compose`. Concretes: `NoOperation` (shared `OperationNoOp` sentinel = Node `Operation.NO_OP`; isNoOp; store→`{}`), `AddFileOperation` (path+File; applyTo→`snapshot.addFile(path, file.clone())`), `MoveFileOperation` (path+newPath; `IsRemoveFile` = new==""), `EditFileOperation` (path + an `EditOperation`; composable when same path AND inner composable), `SetFileMetadataOperation` (path+metadata; applyTo no-ops on a missing file). All expose the base defaults (`CanBeComposedWith*`→false, `Compose`→error "not implemented") that Node's `Operation` base provides.
  - `operation_transform.go`: `OperationAddFile`/`EditFile`/`MoveFile`/`RemoveFile`/`SetFileMetadata` constructors; `OperationFromRaw` (key-presence dispatch: `file`→Add, `textOperation|commentId|deleteComment|noOp`→Edit, `newPathname`→Move, `metadata`→Set, empty→NoOp, else error); `OperationTransform(a,b)` = the 4×4 matrix dispatching to the 12 named `transform*` functions with Node's **transpose rule** — for the reverse-direction pairs, Go computes `f(b,a)` then SWAPS the two results, exactly matching Node's `transpose(F)(x,y)=F(y,x).reverse()`. Pinned oracles: add/add same-path→`[NO_OP,add2]`; add/move relocate; move/move 15-case equivalence (same/divergent/convergent/opposite/transitive/no-conflict); move/rename re-homes the edit; edit/edit delegates to `TransformEditOps`; set/set same→`[NO_OP,set2]`. `OperationTransformMultiple(as,bs)` mutates in place.
  - `rebase.go`: `RebaseChanges(ours, theirs)` — for each theirs-change, `OperationTransformMultiple(our.ops, their.ops)` for each ours-change (a rename CHAIN `a→b→c` only resolves if the 2nd theirs-change is applied to the already-transformed op); then prunes no-op ops and DROPS emptied changes (order preserved). Preserves origin/v2Authors/timestamp (a resend-identity oracle pins these).
  - **Oracle (Node, all passing):** origin 13 + snapshot 12 + change 4 + rebase 10 + operation/transform 57 = the full concurrent-edit surface. Go mirrors all as green tests (`core_origin_test.go`, `core_snapshot_test.go`, `core_change_test.go`, `core_operation_test.go`).
  - **Coverage: 88.6%** (above the 85% gate; the Phase C additions are the bulk of the new code). Remaining Phase C (History/Chunk/ChunkResponse/OTClient/schemas/root_doc/history-v1/diff_as_text_operation/doc_updater_compatible_ranges/ot_migration_stages) are follow-ups.

- (LIB-15 Phase C slice 2) **otc — change identity + doc-updater ranges + file-tree diff**, 1:1 into `go/libraries/otc`. Pure-logic surfaces built on Phase A/B/C1 (all deps already ported). New deps: none (stdlib only + existing otc). Package coverage 88.6%→**89.0%**.
  - `change_identity.go` (Node change_identity.js): `EditorChangeIdentity{HistoryClientId, Author *string (nil=anonymous), Timestamp int64 (Unix ms)}` + `ChangeIdentityFromRaw(raw)` (nil unless origin.kind==`editor`, historyClientId non-empty, v2Authors is exactly one, timestamp parseable) + `ChangeIdentityOf(hc, author, ts)` + `IsSameEditorChange(a,b)` (nil never matches) + `IsChangeFrom(raw, originKind, author, ts)` (writer counterpart: kind + author-in-v2Authors + instant). Timestamp compared as an instant (Unix ms) not a string — two spellings of the same instant match (`rawTimestampMs` handles ISO-string + numeric). NOTE: Go can't share a name between the `EditorChangeIdentity` type and the from-raw fn (Node's typedef+fn do), so the fns are `ChangeIdentityFromRaw`/`ChangeIdentityOf`.
  - `doc_updater_compatible_ranges.go` (Node doc_updater_compatible_ranges.js): `GetDocUpdaterCompatibleRanges(file)` → `{Changes, Comments}`. Non-editable (binary) → empty; editable + null content → err `Unable to read file contents`. Translates tracked changes to `{p, i|d}` (positions shifted left by prior deletes) and comments to `{p, c, t, resolved}` (id) by walking the content and skipping/truncating over tracked deletions (the comment-overlap walk is a direct port). Built on the already-ported `File`/`TrackedChangeList.AsSorted`/`CommentList.ToArray`/`Range`/`TrackingProps`/`toISOString`.
  - `file_tree_diff.go` (Node file_tree_diff.js): `BuildFileTreeDiff(changes, opts)` → `FileTreeDiff{Entries map, Order []string, Removed []}`. Node's `Map<string,entry>` insertion-order is PART of the contract ("entries reported in this order, followed by pathnames the window makes use of") — Go reproduces it with an explicit `Order` slice alongside the `Entries` map (Go maps are unordered). The fold follows FileMap semantics (add/move onto an occupied pathname replaces + reports the replaced file as removed). `seeded` = `opts.InitialPathnames != nil` (distinguishes Node `undefined` vs `[]`). `getLiveEntry`/`removeLiveEntry` preserve Node's "removed without freeing the pathname" semantics. `FileTreeDiffEntry{Origin *string (nil=added-in-window), Chain []string, Edited bool, FirstEditedAtChainIndex *int, File *File, DeletedAtChangeIndex *int}`.
  - **Oracle (Node, all mirrored green):** change_identity (recognises/refuses-anon/timestamp-of/identityOf/never-null) + doc_updater_compatible_ranges (shift-following deletes, comment-truncate `ck bown fox jumps ovzy`, resolved status, thread id, detached→zero-len, entirely-in-delete) + file_tree_diff (20 table cases: untouched/chain-move/move-then-remove/add-then-move/add-then-remove/remove-then-add/moved-onto/added-over/moved-onto-removed/edit-after-move/move-then-edit/first-of-few/entry-on-edit/ignore-edit-removed/swap/skip-no-file×2/skip-no-pathname/ignore-noop/fold-in-order) + onMoveCollision.

- (LIB-15 Phase C slice 3) **otc — root_doc**, 1:1 into `go/libraries/otc` (all deps already ported: file_metadata / Operation / text_file_defaults; pure logic + one `OError`). Coverage 89.0%→**89.1%**.
  - `root_doc.go`: `IsRootDocCandidate(pathname, content)` (root-doc extension — `(?i)\\.(tex|Rtex|ltx|Rnw)$` built from `DefaultRootDocExtensions` — plus a `'^\\s*\\\\documentclass'` line at the start of a doc, first 30 KiB only, each line only from its beginning) + `SetMainPathnameOperations(snapshot, pathname)` (unset `main` everywhere + set it here; refuses non-doc metadata via `oerror` "only a doc can be the root doc") + `ChooseRootDoc(snapshot, candidates)` → `RootDocChoice{Pathname, Operations}` (nearest-to-root, alphabetical tie-break; `nil` where none / already recorded). `chooseRootDoc` is `nil`-or-choice (Go: `(*RootDocChoice, error)`); Node's `metadataOf` is the unexported `rdMetadataOf`.
  - **Oracle (Node, all mirrored green):** isRootDocCandidate (5: class / no-class / wrong-name / indented / commented-out / `\\input{…}` / past-prefix) + chooseRootDoc (6: only-candidate ops shape `{pathname,metadata:{main:true}}` / prefer-beside-project / order-independent / already-recorded→nil / nothing-to-compile→nil / drops-unknown) + setMainPathnameOperations (4: moves-record / nothing-to-do→[] / keeps-other-flags `{main,mainBibliography}` / refuses-non-doc throws).

- (owner 2026-09-21) **Commit bookkeeping**: L01 oerror / L03 streamutils / L04 validtools / L05 settings were DONE+green but LEFT UNCOMMITTED (owner flag: "some go/libraries are not committed yet"), and L09 notifprefs was green but uncommitted. Committed per-package (green) in this session: oerror 96.8%, streamutils 98.6%, validtools 98.9%, settings 98.6%, fetchutils 94.9%, notifprefs 69.2%. rangestracker (L06) was already committed (7bc883cd6e). accesstoken (L08) was RED (1 failing golden test — wrong test password, see LIB-08 entry) and intentionally NOT committed; now green (95.4%) and committed `cf2ecca5d0`. rediswrapper (L10) green (95.3%) and committed (hash in commit log). All commits stage `go/libraries/<pkg>` + HANDOFF.md explicitly (no `git add -A`).

- (LIB-15 otc Phase C slice 4) **otc — diff_as_text_operation + build_set_content_operations**, 1:1 into `go/libraries/otc` (deps already ported: StringFileData/TrackedChangeList/TrackingProps/Operation family/File/BlobStore/TextOperation; the DMP diff comes from a Go dependency). Coverage 89.1%→**89.0%**. **NEW DIRECT Go DEPENDENCY: `github.com/sergi/go-diff v1.4.0`** (owner-approved 2026-09-21) — see the decision entry below.
  - `diff_as_text_operation.go`: `DiffAsTextOperation(file, after, opts)` + `DiffsToTextOperation(file, diffs, opts)` + the `ADDED`/`REMOVED`/`UNCHANGED` constants. Computes the DMP diff against the file's tracked-deletes-filtered content and rebuilds a `TextOperation` over the FULL content that re-inserts the tracked deletes at their correct (shifted) positions. The tracked-change walk is a direct port of Node's `tcIndex` while-loop (Go `break` must exit the FOR, not the switch — the `tcLoop:` label is that pin). Pinned errors: `Unknown type` and `StringFileData.trackedChanges out of sync: unexpected range after end of diff`.
  - `build_set_content_operations.go`: `BuildSetContentOperations(ctx, BuildSetContentArgs)` → `{Operations, Status}` (a setDoc-like upsert). Exactly-one-of content/blob; editable existing file → in-place minimal-diff edit (editFile) + optional setFileMetadata when metadata differs; otherwise putString→blob, hash/noop short-circuit, optional whole-doc tracked-insert ranges blob, removeFile+addFile. Pinned error: `buildSetContentOperations: exactly one of content and blob must be given`. `_.isEqual(file.getMetadata(), metadata)` → `reflect.DeepEqual` on the JSON-like maps; Node's `metadata = {}` default → `nullSafeMeta`.
  - **Oracle (Node, all mirrored green):** diff_as_text_operation (noop / minimal-edit / diffs-against-filtered-deletes / tracked-insert / tracked-delete / convert-insert / convert-remove / whole-doc-delete / unknown-type-throws / retain-delete-in-unchanged|removed / retain-deletes-after-end / out-of-sync-throws / leaves-existing-deletes / remove-inside-tracked-insert / split-removal-over-tracked-insert / straddling-tracked-insert) + build_set_content_operations (20: exactly-one-of / minimal-diff / lazy-load-then-diff / identical-noop / clear-metadata / set-metadata×2 / same-content-md-only / tracked-whole-doc-delete / binary→doc / binary-identical-noop / create-new / tracked-insert-ranges / empty-tracked-no-ranges / blob-replace / doc→binary / blob-same-md-noop / blob-md-only / blob-add-new / blob-ignores-tracking) + `dmp_test.go` interop vs Node `diff-match-patch@1.0.5` (noop / insert-brave / remove-cruel / multiline / multiline-tail / sentence / block-replace).

- (owner 2026-09-21) **NEW Go dependency for otc DMP: `github.com/sergi/go-diff v1.4.0`** (a faithful Go port of Google `diff-match-patch`). Owner approval: otc's `diff_as_text_operation` needs DMP's diff (`diff_main` + `diff_cleanupSemantic`) and hand-porting DMP's 3-line merge algorithms into this 1:1 program was rejected in favour of a maintained, Apache-2.0, zero-runtime-dep Go package. Pinned to **v1.4.0** (the owner-supplied reference clone `/data_1/image_mining/the_diff/go-diff` is v1.4.0; proxy v1.4.0 matches it for `diffmatchpatch/diff.go`). **Interop gate (green):** `go/libraries/otc/dmp_test.go` asserts `sergi/go-diff@v1.4.0` output == Node `diff-match-patch@1.0.5` output for the pinned corpus (no-op, single insert, single delete, line-boundary inserts, a 12-line block replace). `Diff_Timeout=0.1` → `DiffTimeout=100ms`. go-diff is now a DIRECT require in `go.mod` (was indirect on first `go get`; normalised by `go mod tidy`). Usage confined to otc's diff surface (`DiffMain(text1,text2,true)` + `DiffCleanupSemantic`); no other package imports it.

## KNOWN DIVERGENCES / RISKS (fill as they appear)

- (LIB-07) fetchutils: (a) Go net/http **server** drains a request body before flushing an early response (express responds immediately) — so the "destroy request body if not consumed" oracle test uses a raw-TCP early responder, not the stdlib test server; client-side destroy semantics are 1:1 (bodyPipe); (b) abort (Node AbortController) → context cancellation; Go does not Unwrap non-OError causes into the oerror stack (pinned in oerror), so cancel is asserted via the transport error text; (c) x509 codes: Go `UnknownAuthorityError` (untrusted/self-signed) → Node `DEPTH_ZERO_SELF_SIGNED_CERT`; `CertificateInvalidError(Expired)` → `ERR_CERT_EXPIRED` — other reasons render the Go message (no Node code exists in scope); (d) Go 1.27 has no `SelfSigned` InvalidReason — self-signed-untrusted surfaces as `UnknownAuthorityError` (handled as above); (e) `errors.As` needs VALUE-type targets for x509 value-receiver error types (pointer targets miss them).
- (LIB-06) rangestracker: (a) Node `comment.op.c === undefined` throws TypeError on `.length`; Go treats nil `C` as `""` (unreachable for schema-valid data — pinned); (b) `UnionVal` short-circuits at the first clean arm (Node zod evaluates all arms) — failure verdict and clean-arm value are identical, only the failing-issue detail group would differ; (c) `GetChanges`/`New` copy values at the boundary (Node hands out object refs) — live mutation parity is kept INTERNAL (elements + dirty state) so the observable flush-time reads match; (d) `sortChangesStable` uses `sort.SliceStable` (V8 `Array.sort` is stable since ES2019) — equal-key order preserved; (e) seed generation uses math/rand/v2 vs Node Math.random — both non-deterministic, byte-SHAPE 1:1 (18 hex seed, 6-hex increment, zero-padded).

- (LIB-01) oerror: divergences (a)-(e) above — all exercised by tests; no silent behaviour change.
- (LIB-05) settings: (a) `merge`'s V8-key-ordered partial-apply on a `null`-override throw is NOT pinned (Go canonical-numeric-then-lex order is documented, pre-throw partial state is V8-implementation-dependent); (b) `NullOverrideError` carries a `Path` the raw Node TypeError lacks (Go convenience, root-cause text stays verbatim); (c) `Load` seams (`EnvLookup`/`Filesystem`/`ReadModule`) replace Node's PnP `require` + `fs.existsSync` + `process.env` — the injected values MUST be the consuming service's real modules/config files, not re-implementations; (d) `moduleWithMerge` is checked on the DEFAULTS module value (a Go value implementing `MergeWith(any) any`), not a Go-reflect probe like Node's `typeof === 'function'`.
- (init) oerror tests found a real semantic bug: the Node suite checks
  `getFullInfo(error.cause)`, and Go `Tag` tags the *OError in place — the
  wrapper and cause are the same object after `Tag(bar, ...)`. Documented in
  the test.

## NEXT-SESSION PLAYBOOK

1. `TODO` list (tool `todo`): index `b5890b9a`; claim the next ○ LIB task and set
   status `in-progress`.
2. Run the green gate first: `go build ./... && go vet ./... && gofmt -l . && go test ./... -cover` (repo root).
3. Pick the topmost ◐/next ○ from the STATUS table; read the Node source (lib
   `index.js` + `test/` mocha files) before writing Go — the Node test file is the
   acceptance spec.
4. Port → golden-parity tests → coverage floor per todo → update table + commit
   (message: `go/libraries LNN <name>: <what> (<cover>%, <oracle> tests)`).
5. VRAM discipline: one lib per context window; no bulk multi-lib edits; keep this
   doc's STATUS line and table current *before* you stop.
6. **`git add -A` GUARD**: this repo currently has *untracked* in-progress
   work from the separate B3 opmodel track (`services/project-history.go/
   internal/opmodel/*`). NEVER `git add -A` / `git commit -a` — stage only
   `go/libraries/<pkg>` + `go/libraries/HANDOFF.md`. The B3 track commits
   its own files itself. (It was accidentally swept once during L03 and
   reset out; those files remain on disk, untracked.)
   doc's STATUS line and table current *before* you stop.

- (LIB-03) streamutils divergence pin: Node `IncrementalResponse#end` double-call (`res.end()` then `res.destroy()` fallback) collapses to a single `Finish()` here (no Go analogue for the destroy fallback; failing finish on a finished response is observable-equal). `PassThrough` (Node read-side + write-side) ports to the read-side wrapper (documented above).
