# TODO — OlliTeX /hub: fix the 33 issues → repurpose E2E → remove old pages

Date: 2026-09-07 (night plan, autonomous execution; owner asleep 8-10h+)
Branch: `ext-6.3.0-port` · last commit: `83ebf4a4d9` (unified /hub + Appearance + toast/persistence fixes)
Ledger (`tests/e2e/hub-issues.md`): **34/34 filed (0-33), P0-P3 tagged, execution waves A/B/C defined.**
NOTE: owner is testing the **production** box (`psintern`); my `make all` updates the shared image they cycle (`compose_cep`), test-stack cycles stay local-only.

## Decision (owner question, answered 2026-09-07)

**When to adapt the old E2E tests to /hub: NOT before the 33 issues are fixed.**
The tests encode behavior; with 33 known-defect surfaces still moving, repurposing
now means rewriting specs twice. Order:

> **1) fix the 33  →  2) green gate  →  3) repurpose E2E onto /hub  →  4) remove old pages (suite justifies it)**

Evidence: zero specs currently cover `/hub` (they drive the legacy pages the 33
issues live on). No `/hub/admin` or `/hub/workspace` references in the suite.

## Spec repurpose map (phase 3)

| Spec (currently drives)            | /hub target(s)                                          |
|------------------------------------|----------------------------------------------------------|
| `admin-site` (`/admin/site`, `/user/llm-settings`) | `#/site/general/misc`, `#/site/llm/*`, `#/mysettings` |
| `python-runner` (`/admin/site-settings`)           | `#/site/general/misc` (misc.pythonRunner)               |
| `keybindings` (`/user/mysettings`)                 | `#/mysettings/keybindings`                              |
| `notifications` (`/user/notification-preferences`) | `#/mysettings/email`                                    |
| `byo-llm`, `grammar` (`/user/llm-settings`)        | `#/mysettings/llm/*`                                    |
| `sync-graceful` (`/user/mysettings` webdav/dropbox)| `#/site/compilation/webdav|dropbox`                     |
| `zotero` (`/user/zotero`, `/library`)              | `#/integrations/zotero`, `#/library`                    |
| `smoke`, `editor-console-canary`  | unchanged (editor)                                       |

New specs to add (phase 3b):
- deep links + auto-expand of active branch
- admin rail visible for admin, hidden for member (both accounts)
- Appearance: apply → live theme both modes → persist → reset → export/import
- `/hub/admin` + `/hub/workspace` → 302 `/hub`

## Night execution plan

| # | Phase | Work | Exit gate |
|---|-------|------|-----------|
| 0 | Triage | File all 33 in `tests/e2e/hub-issues.md` (repro/steps/expected/actual/severity/status). Owner list = source of truth; merge self-audit findings | 33/33 rows with repro steps |
| 1 | Fix   | Batches of 5–8 issues; build + targeted probe per batch; no half-fixed UI states | batch green, ledger rows flipped |
| 2 | Green gate | `make all` green · probe matrix (every leaf × admin/member × light/dark + console/4xx/5xx) · current 22-spec E2E green | all green + commit |
| 3 | Repurpose | Rework the specs in the map to drive the same flows through /hub; add the 5 new specs | suite green on /hub paths |
| 4 | Removal | Delete old `/hub/admin` + `/hub/workspace` routes/entries/pug (redirects KEEP); delete legacy page shells only if referenced by no spec | full suite green + commit |
| 5 | Report | `HUB_NIGHT_REPORT.md` (repo root): found → fixed → evidence → open items; update `next_steps.md` | file written |

## Guardrails (all night, unattended)

- **No push, no production touch** (no compose_cep cycle); local commits only.
- Test stack only: `ol-e2e-*` / 127.0.0.1:7420. Credentials never in logs/files.
- Every phase behind an explicit green gate; every phase = its own commit (revertable).
- Design-level uncertainty → park in the ledger with analysis, don't guess, keep going.
- Timeouts on every long command; keep the `make all` log at /tmp/build-*.log.

## Status

- [x] Phase 0 — ledger: 34/34
- [ ] Phase 1 — fixes: 0/34 (WAVE A/B/C order)
- [ ] Phase 2 — gate
- [ ] Phase 3 — specs on /hub
- [ ] Phase 4 — old page removal
- [ ] Phase 5 — morning report

## Environment cheat-sheet

- Build: `cd server-ce && timeout 2300 make all` (log /tmp/build-*.log)
- Cycle test stack: `cd tests/e2e && timeout 300 docker compose -f docker-compose.test.yml --env-file .env.test --project-name ol-e2e up -d overleaf` then poll `curl -s -o /dev/null -w "%{http_code}" http://127.0.0.1:7420/login` until `200`/`302`
- E2E: `cd tests/e2e && npx playwright test`
- Logs: `docker exec ol-e2e-overleaf-1 sh -lc 'tail -60 /var/log/overleaf/web.log'`
- Mongo (test stack, no auth): `docker exec ol-e2e-mongo-1 mongosh --quiet mongodb://127.0.0.1:27017/sharelatex --eval '...'` (db `sharelatex`, **plural** collections)
- Admin `e2e-admin@e2e.test` / member `e2e-user@e2e.test` (fixatures in `tests/e2e/fixture-pw.md` — never echo)
